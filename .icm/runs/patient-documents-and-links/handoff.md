# Handoff: patient-documents-and-links

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged and archived; nothing to pick up. The next stub in `patient-loop` is
   `general-feedback`; the P0 chore `dependency-audit-next-rce` is waiting in triage.

## Blockers

- none

## Do not

- Do not reopen this run — a change to what shipped is a new stub.
