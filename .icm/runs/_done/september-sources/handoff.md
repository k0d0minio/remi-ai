# Handoff: september-sources

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Review `01_scope/output/scope.md` (the addendum: D-16 … D-24, Out of scope, Open for Define)
   and the amended breakdowns: `.icm/intake/patient-loop/breakdown.md`,
   `.icm/intake/beyond-december/breakdown.md`, `.icm/intake/ai-assist/breakdown.md`.
2. Confirm row 5? of `.icm/docs/README.md § Precedence` (the brainstorm) — still provisional.
3. `new` — the next stub is `patient-loop/challenges`; then `patient-documents-and-links` (the files
   seam: a Vercel Blob store and its token must exist on the Vercel team before Build), then
   `general-feedback`.
4. The operator drops more raw input later: `.icm/scripts/process-raw.sh`, then `scope` the
   pointer stubs it parks.

## Blockers

- none — this front pushed to a `claude/` branch and a PR rather than straight to `main` (the
  session's branch rule), so the review surface is the PR, not `main`.

## Do not

- Do not re-scope the six already-filed documents the drop duplicated — their pointer stubs are
  retired with a `superseded-by:` line naming where each is filed.
- Do not edit `01_scope/_source/story.md` — the source is frozen.
- Do not pick `beyond-december/*` — every stub there is P2 until the owner moves it.
