# Build notes: profile-fields

- commits: `feat(services)` — the five § A columns and the constraints narrowing; `feat(admin)` —
  the five fields on the profile form
- demo: none

## What changed

- `packages/services/src/shared/patient.ts`: `cookingAffinities` — `yes | somewhat | no`, the
  closed set `likes_cooking` derives from. Not recorded is `null`, not a fourth member: that is the
  shape `consentChannel` already uses, and two spellings of "unknown" is the bug it avoids.
- `packages/services/src/db/schema.ts`: five columns — `dietary_regime`, `allergies`,
  `intolerances`, `food_budget` (all `not null default ''`) and `likes_cooking` (nullable).
  `constraints` keeps its name, type and content; only its comment narrows to the medical
  constraints that are neither allergy nor intolerance.
- `packages/services/src/db/migrations/0003_superb_baron_strucker.sql`: generated by
  `pnpm db:generate`, never hand-written. Five `ADD COLUMN`s with defaults — every existing row is
  valid the moment it runs, so no backfill and no manual step.
- `packages/services/src/db/models/patient-profile.ts` and the two barrels: the five fields and
  `CookingAffinity`, derived from the constant.
- `packages/services/src/db/services/patients/index.ts`: the five in `patientFields`, in the create
  defaults, and in the update path. `nullableChannel` was a `ConsentChannel`-shaped clone of
  `nullableText`; a second copy for the affinity would have been a third, so it is now one generic
  `nullableEnum` and the clone is deleted.
- `apps/admin/components/patients/vocabulary.ts`: `cookingAffinityLabels` (oui / un peu / non) and
  `dietaryRegimeSuggestions` — typing aids only, not a closed set.
- `apps/admin/components/patients/patient-form.tsx`: regime as an Input with a `datalist`,
  allergies and intolerances side by side above the narrowed « Contraintes médicales », then the
  affinity Select and the budget Input under preferences. The `datalist` sits outside `Field` on
  purpose: `Field` clones a single child to wire `aria-describedby`, so a second element would
  silently drop the hint from the accessibility tree.
- `apps/admin/lib/patients/actions.ts`: the five collected in `patientInputFrom`, the affinity
  narrowed by `asCookingAffinity` the same way the consent channel is. The audit call is untouched.

## Acceptance criteria status

- [x] Five columns plus a generated migration — `0003_superb_baron_strucker.sql`, pure `ADD COLUMN`.
- [x] `PatientProfile` carries them, and `likesCooking` derives from `cookingAffinities` — the same
      pattern as `patientSexes` and `consentChannels`.
- [x] The service validates and persists on create and update; a partial update leaves the rest
      untouched and an empty string clears. Covered by tests.
- [x] The form shows all five with French labels and hints, saves through `savePatientAction`, and
      re-renders them on reload.
- [x] Regime is free text with `datalist` suggestions; the affinity is a Select over the constant
      with French labels; the budget is a plain Input.
- [x] `constraints` is unchanged in name, type and content — only the schema comment, the model
      comment and the form hint narrow.
- [x] The audit entry is untouched: the save action still records `patient.updated` on success,
      unchanged in shape.
- [x] Tests round-trip all five against the in-memory client, including each of the three affinity
      values and the not-recorded case. 86 tests pass in `packages/services`.
- [x] `/p/[token]` is untouched — no file under `apps/web` is in the diff.

## Notes for Verify

- The patient link now shows less than its label implies, and that is deliberate. `/p/[token]`
  renders `constraints` under « À éviter ». Allergies and intolerances recorded in the new fields
  will not appear there. Surfacing them is the `patient-surface` epic's call — spec § Out of scope,
  epic decision 4 — but the wording drifts until that lands.
- No migration of existing prose, by design. Current `constraints` text stays where it is; Morgane
  redistributes it as she next touches each of the 10–15 profiles.
- `nullableChannel` becoming `nullableEnum` is a rename plus a widened type, not a behaviour
  change; the consent-channel tests cover the old path unchanged.
- The stub's fourth open question — whether `preferences` splits into liked and disliked foods — is
  still open and still Morgane's. Nothing here assumes either shape.
- Local runs: `npx vitest run` in `packages/services`, 86 passed. Format, lint, typecheck and the
  preview build are the factory's.
