# Build notes: nutrition-rules

- commits: see the branch — services (table, service, tests), `@remi/ui` (the Markdown primitive), admin (« Connaissances »)
- ci: pending — filled in after the last push

## What changed

### `packages/services`

- `src/shared/nutrition.ts` (new): `nutritionRuleKinds` and `nutritionRuleStatuses`, the two closed
  vocabularies, plus their derived types. In `shared/` rather than beside the model because
  `db/models/` is types only — a runtime list there would join every client bundle.
- `src/shared/audit.ts`: six new actions — `nutrition_rule.{created,updated,revised,validated,archived,restored}`.
- `src/db/models/nutrition-rule.ts` (new): the `NutritionRule` type. Three separate "not in force"
  states, documented in the file, because each answers a different question.
- `src/db/schema.ts`: the `nutrition_rules` table. No `practitioner_id` (Out of scope), no foreign
  keys — `validated_by` follows `audit_events.actor_id` so the record survives an account being
  removed, and `superseded_by` is a same-table pointer that cannot dangle because nothing is ever
  deleted.
- `src/db/migrations/0013_fair_may_parker.sql`: generated by `drizzle-kit`, then the seed appended
  as a second statement (see **The seed** below).
- `src/db/services/nutrition-rules/` (new): the corpus service and `retrieveNutritionRules`.
- `src/server/index.ts`: the new surface, exported.

**`src/db/adapters/neon.ts` needed no edit**, though the spec's `touches:` listed it. The adapter
derives its collection map from every `pgTable` exported by `schema.ts`, so defining the table
registered it. That derivation replaced a hand-kept list precisely so a table could not be
forgotten; this run is the first one to benefit.

### `packages/ui`

- `src/server/markdown.tsx` (new) + `src/server.ts`: the `Markdown` primitive, server-safe.
- `pnpm-workspace.yaml` + `packages/ui/package.json`: `unified`, `remark-parse`, `remark-gfm`,
  `remark-rehype` and `hast-util-to-jsx-runtime` via the catalog; no app pins its own. **Not
  `react-markdown` — see below.**
- `tsup.config.ts` is unchanged: tsup externalises a package's own `dependencies` by default, so
  nothing had to be said.

### `apps/admin`

- `app/(admin)/knowledge/` (new): the list and the detail page.
- `components/knowledge/` (new): `rule-form`, `rule-rows`, `rule-actions`, `corpus-filters`.
- `lib/knowledge/actions.ts`, `lib/knowledge/vocabulary.ts` (new).
- `components/shell/nav-sections.ts`: « Connaissances » beside « Recettes », no `ownerOnly`.
- `components/audit/vocabulary.ts`: the six French labels and their intents.

## The seed

Nine draft rows, inserted by the same migration that creates the table — migrations run once, so
the seed needs no script and no environment variable, and it is guarded on an empty table so a
manual re-apply cannot duplicate it. **Every row is `draft`.** Five carry real content quoted from
the v2 structure brainstorm (§§ G, H, I) with the source line in the body; four are skeletons
marked « à compléter » for the food lists § H names and never fills — oméga-3, fibres, polyphénols,
diversité végétale.

The spec's finding held up in the build: **`.icm/docs/braindump/` contains no nutrition content.**
A grep for oméga-3, polyphénols, fibres, magnésium and anti-inflammatoire across all seven of its
folders returns nothing; they are business, marketing, roadmap, vision and journal material. The
whole of what there was to seed is the brainstorm and the correspondence.

## Acceptance criteria status

- [x] `nutrition_rules` behind the storage seam with every named column — `drizzle-kit` generated
      `0013_fair_may_parker.sql` and it is committed.
- [x] Every write goes through `@remi/services`; no action reaches the database directly; there is
      no hard delete in the service or on the page.
- [x] Editing a validated rule opens a new draft at `version + 1` and sets `supersededBy` on the
      old row, which keeps its status and its validation stamp; a draft edits in place.
- [x] `retrieveNutritionRules` filters to validated-current-unarchived with ≥ 1 shared tag, orders
      by overlap → `validatedAt` desc → `id` asc, caps at `NUTRITION_RULE_RETRIEVAL_LIMIT` (8) which
      `limit` lowers but cannot raise, and returns `[]` for an empty query.
- [x] In-memory tests over `createMemoryDatabase()` cover all eight named cases, each in its own tag
      namespace so the shared corpus cannot leak between them.
- [x] `/knowledge` lists with tag, kind and status filters and a title search; superseded and
      archived rows are behind an explicit shelf selector.
- [x] The detail page renders the body through `Markdown` from `@remi/ui/server` and shows kind,
      status, tags, version, who validated it and when, with links to the versions either side.
- [x] Any signed-in operator can create, edit, validate, archive and restore; each writes an audit
      row; every new action name has a French label (the closed map makes a miss a type error).
- [x] « Connaissances » is in the `Suivi` nav section beside « Recettes », no `ownerOnly`.
- [x] The renderer's packages are in the catalog, consumed only by `packages/ui`, raw HTML not
      enabled, no app pinning its own. **The criterion named `react-markdown` and now does not** —
      `spec.md` carries the revision and the reason; the component's placement, API and behaviour
      are unchanged.
- [x] The migration seeds nine draft rows as described; no food list or nutrient claim was written
      by us.
- [x] No model, provider or AI call anywhere in the diff.

## Notes for Release

- **Smoke-test order on the preview.** Create a rule → validate it → edit it (this must land you on
  a _new_ page at version 2, with the old one still reading « validée ») → validate the new version
  → archive and restore it. The version links at the top of the detail page are the quickest way to
  see the chain is intact.
- **The deliberate gap.** Between a revision and its validation, neither row is retrievable. The
  form says so above the button, and a test asserts it. Worth confirming that is the behaviour
  Morgane wants — the alternative (keep the old wording live until the new one is validated) is a
  small change to `updateNutritionRule`, not a reshape.
- **The seed only appears where migrations run.** A preview without `DATABASE_URL` skips them, and
  `migrate.mjs` refuses to migrate from a non-production Vercel deploy. On a preview pointed at a
  migrated database the nine drafts will be there; on one without, `/knowledge` renders its empty
  state, which is correct rather than broken.
- **The renderer changed underneath, and it cost a CI round.** The first push used
  `react-markdown`. Its module imports `useEffect`/`useState` at the top level to build its
  `MarkdownHooks` export, and React's `react-server` build exports neither, so the module cannot
  link in a server component's graph — importing it is enough. Five previews passed and `admin`,
  the only app that renders markdown, failed.

  The two remedies I expected both failed on inspection: bundling it does **not** tree-shake the
  unused hook import (it survives, and `dist/server.js` goes from 33 KB to 457 KB), and the default
  import was correct all along. The pipeline `react-markdown` itself wraps is used directly
  instead, none of it importing React outside `react/jsx-runtime`.

  Proved rather than assumed, and worth re-running if this area is touched again: load the built
  bundle under the failing condition —
  `node --conditions=react-server -e 'import("./packages/ui/dist/server.js")'`. Before the change it
  threw on the missing specifiers; after it, `Markdown` renders, raw HTML is dropped and GFM tables
  come through.

- **Two open questions are still open, by design** (spec § Open questions): her tag vocabulary, and
  whether the corpus is house-wide or per-practitioner. Neither blocks anything here. The four
  « à compléter » rows are the first thing to put in front of her.

Context budget: within the Build Inputs table, plus the `recipes` service, page and components as
the pattern this run follows, and `remi-v2-structure-brainstorm.docx` §§ G–I for the seed text.
